---
description: update jujutsu description if jujutsu vcs is used
applyTo: '*'
---
If there is a `.jj` subdir in the workspace root, run `jj describe --no-pager -m 'add a brief description here'` after every change (not after every edit, just when you finish a meaningful)
